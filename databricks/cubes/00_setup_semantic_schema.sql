-- ═══════════════════════════════════════════════════════════════
-- Setup: Create procurement_semantic schema if it does not exist
-- Must run before any cube views are created
-- ═══════════════════════════════════════════════════════════════

CREATE SCHEMA IF NOT EXISTS workspace.procurement_semantic;
